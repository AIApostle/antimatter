# Agent Instructions

## Git Commit Guidelines
- Always commit changes to Git when you are done with a task.
- Use clear, descriptive commit titles following conventional commit style (e.g., `feat: ...`, `fix: ...`, `chore: ...`).
- Ensure all relevant modified and new files are staged appropriately before committing.
- Do not commit secrets, environment credentials (`.env`), or sensitive artifacts.
